import type { ReactNode } from 'react';

export const metadata = {
  title: "Percentage to CGPA Calculator for final year college students| Toolup",
  description:
    "Convert your percentage into CGPA instantly using the standard Indian university formula. Free Percentage to CGPA calculator for college students.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
