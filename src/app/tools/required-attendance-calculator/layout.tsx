import type { ReactNode } from 'react';

export const metadata = {
  title: "Required Attendance Percentage Calculator for College Students | Toolup",
  description:
    "Calculate how many more classes you need to attend to reach the required attendance percentage. Free required attendance calculator for Indian colleges and universities.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
