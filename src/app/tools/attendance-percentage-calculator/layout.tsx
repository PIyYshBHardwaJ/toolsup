import type { ReactNode } from 'react';

export const metadata = {
  title: "Attendance Percentage Calculator for College Students | Toolup",
  description:
    "Calculate your attendance percentage easily using total classes and classes attended. Free attendance calculator for Indian colleges and universities.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
